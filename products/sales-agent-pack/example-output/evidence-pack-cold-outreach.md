# Worked example: sales-copywriter applied to the Evidence Pack

This is what the `sales-copywriter` agent produces when pointed at a real offer — the "znak-orient Evidence Pack for AI Coding Agents" from `docs/MONETIZATION.md` (Track B) — for a real, narrow audience (developers who use Claude Code/Codex/Cursor across multi-session projects). It follows the agent's own rules: problem first, benefits not features, one CTA, no fabricated claims.

Not sent anywhere. This is a draft for a human to review, adapt to the actual recipient, and send.

---

**Subject: Your AI agent forgot what it already tried**

Hi [name],

Quick question: has an AI coding session ever told you something was done, and it wasn't — because a test from two sessions ago actually failed, and nothing carried that forward?

That's what the znak-orient Evidence Pack fixes. It's a working template (plus the generator script that built it) for tracking multi-session AI agent work so that a failed test stays a failed test, a decision one session made doesn't get silently overridden by the next one, and a "looks done to me" chat message never gets treated as proof.

It's not a new tool to learn — it's a ready evidence-package template you drop into your own project and adapt, with a playbook walking through exactly how. Verified end-to-end against the actual open-source engine (121/121 tests, deterministic pass) — you can check that yourself, the whole thing is on GitHub.

$[price] one-time, delivered as a download.

Want the link?

[your name]

---

**Why each part is there** (per the agent's rules): opens with the reader's own situation, not the product name; "forgot what it already tried" is the cost of inaction, stated before any pitch; every claim (121/121 tests, deterministic) is something the recipient can verify themselves rather than an adjective; one CTA ("want the link?") instead of stacking a purchase link, a GitHub link, and a reply-to-learn-more all at once.
