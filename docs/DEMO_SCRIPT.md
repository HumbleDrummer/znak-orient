# Narrated demo script — 2 minutes 35 seconds

Recording status: the retained local `artifacts/znak-orient-demo.webm` is a 35.44-second silent interaction/evidence capture of the same workflow. This longer script is the proposed narration for a future published submission video; no public upload is claimed.

## 00:00–00:20 — Problem

“Project history can be large and still fail to tell us where we are. ZNAK ORIENT turns partial, stale, contradictory, and untrusted evidence into a current position, explicit blockers, and one justified next action.”

Show the local command and open `http://127.0.0.1:8765`.

## 00:20–00:50 — Noise intake

“This judge-safe package contains an old valid checkpoint, a newer success claim, a failed receipt, two conflicting entrypoint decisions, an embedded instruction, an authorized publication gate, a duplicate, a stale update, and an unauthorized override.”

Point to the colored intake dispositions. Click **Rejected** to show five rejected items, then return to **All**.

## 00:50–01:20 — Correct position

“The new success claim does not win. Its validation receipt is FAIL, so the current position remains: clean-checkout validation failed; the project is not judge-ready. FACT confidence and decision authority are separate.”

Point to `BLOCKED`, the source-backed position, and the goal. The integrated ZNAK assistant anchors at the blocker, points to the single selected action, and then becomes still; it is a presentation aid, not a second source of direction.

## 01:20–01:50 — Conflict, unknown, and one step

“Two authorized entrypoint records disagree and neither supersedes the other. ZNAK ORIENT preserves both as DISPUTED, exposes the critical failure-cause unknown, and refuses to execute either command. It returns exactly one corrective step: record an authorized resolution. The success condition is machine-readable.”

Point to both claim sources and the `conflict_resolved` condition.

## 01:50–02:15 — Recovery Card

“The Recovery Card is a restart view derived from the checkpoint. It is visibly marked not source of truth and cannot write back. The forged card in intake was rejected.”

Click **Copy Recovery Card** and point to its checkpoint provenance.

## 02:15–02:35 — Evidence and verification

“Every material output links to source records. The imported receipt remains FAIL. Separately, the local transform receipt is PASS and says exactly what it does not prove. The application runs on loopback with no credentials, model calls, telemetry, or external service.”

End on the Validation Receipt section, then show the retained automated test command output.
