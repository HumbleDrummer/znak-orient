# GitHub publication receipt — 2026-07-19

## Repository

- URL: `https://github.com/HumbleDrummer/znak-orient`
- owner: `HumbleDrummer`
- visibility after completion: `PUBLIC`
- default branch: `main`
- license: MIT

## Sanitized publication baseline

- branch: current `main`
- reachable-history privacy scan: zero matches for the removed local Windows user path and private submission Session ID
- remote-clone gate: complete 121-test suite, deterministic CLI replay, `compileall`, local-link validation, secret scan, and clean worktree
- GitHub Actions: [current CI workflow](https://github.com/HumbleDrummer/znak-orient/actions)

The CI workflow executes the complete 121-test suite and deterministic demo command with Python 3.11 on `windows-latest`, `ubuntu-latest`, and `macos-latest`. Exact final commit, tree, workflow-run URL, remote-clone output, and source-archive hash are retained in the local publication handoff generated after the final workflow completes.

## Evidence boundary

This receipt establishes a real GitHub repository, a sanitized reachable history, a working remote test path, and three-platform CI for the named baseline. The documentation commit containing this receipt is validated by its own GitHub Actions run and the separate local publication handoff receipt.

It does not claim a public hosted application, a compliant narrated YouTube video, `/feedback` submission, Devpost registration, or final Devpost submission. The retained WebM is silent local evidence and remains unsuitable as the required competition video.
